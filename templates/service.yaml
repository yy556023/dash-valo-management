apiVersion: v1
kind: Service
metadata:
  name: {{ include "dash-valo-management.fullname" . }}
  labels:
    {{- include "dash-valo-management.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "dash-valo-management.selectorLabels" . | nindent 4 }}
