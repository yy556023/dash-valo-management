apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dash-valo-management.name" . }}-test-connection"
  labels:
    {{- include "dash-valo-management.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dash-valo-management.name" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
